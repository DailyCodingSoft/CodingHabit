/*
Pendiente de validacion y creacion de la llave 
https://resend.com/domains/12846f82-84fa-4c7c-8be5-365128d1d6c5
correo empresaroal
import { Resend } from "resend";

const resend = new Resend(process.env.RESEND_API_KEY);

export async function sendEmail({
  to,
  subject,
  html,
}: {
  to: string;
  subject: string;
  html: string;
}) {
  return await resend.emails.send({
    from: "TuApp <noreply@dailycoding.online>",
    to,
    subject,
    html,
  });
}*/
